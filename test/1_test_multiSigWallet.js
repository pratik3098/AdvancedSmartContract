const multiSigWallet = artifacts.require('multiSigWallet');

contract("multiSigWallet", accounts => {
   
    it("should be able to send balance to contract", async () => {
          assert(1,1,"proper syntax")    
     });
})